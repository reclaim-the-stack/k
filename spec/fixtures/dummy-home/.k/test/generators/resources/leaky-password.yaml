# Uses %{randomPassword} outside of a Secret, which k generate must refuse.
#
# Example values.yaml:
# env:
#   - { name: SERVICE_PASSWORD, value: "%{randomPassword}" }

apiVersion: v1
kind: Service
metadata:
  name: %{application}-leaky
spec:
  ports:
    - port: 1234
