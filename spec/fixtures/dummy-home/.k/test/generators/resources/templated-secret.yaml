# Uses Helm templating in a Secret, which can't be sealed.
#
# Example values.yaml:
# env: []

apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Chart.Name }}-auth"
  namespace: default
stringData:
  password: %{randomPassword}
